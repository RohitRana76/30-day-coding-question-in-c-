#include<iostream>
#include<cmath>
using namespace std;

int main(){
    int num;
    cout << "enter the number to find its square" << endl;
    cin >> num;

    // int squareOfNumber = pow(num,2);
    cout << "the square of a number is : " <<  pow(num,2) << endl;
    
    return 0;
}
